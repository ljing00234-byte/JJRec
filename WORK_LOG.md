# 工作日志

记录 my-agent 项目搭建过程中的关键决定，及背后的理由。跟 README.md 分开维护——README 说"现在是什么样"，这份文档说"怎么走到这一步、为什么"。

对照目录（只读）：/Users/jj/Downloads/GOAI agent/JKRec
工作目录（可写）：/Users/jj/Desktop/RERand/my-agent

## 2026-09-05

### 决策 1：复现纪律

选择：A —— 全程手打，不复制粘贴 JKRec 里任何文件/代码/配置。
理由：不放心自己能分清"数据"和"逻辑"的界限，用"是不是自己手敲的"这个机械标准代替判断。

### 决策 2：Mac 底子核实

选择：B —— 芯片、python3 版本/来源、Xcode CLT、Homebrew 三项都查清楚。
结果：Apple M5（arm64）；裸命令 python3 不稳定（同一台机器不同会话会解析到 conda 3.14.6 或 Homebrew 3.14.7）；Xcode CLT 与 Homebrew 均已安装；另装了 Homebrew 的 python@3.12（3.12.14）供本项目专用。

### 决策 3：.venv 建立方式

选择：B —— 不改全局 conda 设置，每次干活前手动 conda deactivate，再 activate 项目自己的 .venv。
结果：.venv 用 python3.12 建成功，激活后解释器指向 .venv 内部。

### 决策 4：密钥策略

选择：C —— 现在就建 .gitignore 排除 .venv 和 .env，但不建 .env.example，等真的需要密钥字段时再补。
理由：现阶段判断不需要密钥，但"密钥不能进 git"这道防线值得现在就焊死。

### 决策 5：版本控制启用时机

选择：A —— 现在就 git init，并立刻做第一次提交。
理由：想留下完整的证据链，不想让"什么时候开始留痕"这件事往后拖。

### 决策 6：git 提交身份

选择：B —— 只在本仓库配置身份，不改全局设置。
值：user.name = ygg234，user.email = ljing00234@gmail.com。

### 决策 7：环境验收标准

在原有"能 activate venv、python 能 import、工作路径是 my-agent、对照路径是 JKRec"基础上，加入冒烟测试（.venv 内能装包、能 import）与本文档作为决定留痕的载体。

### 决策 8：冒烟测试结果

在 .venv 中安装 wheel 并成功 import，版本 0.48.0，验证 pip → 安装 → import 整条链路可用。

### 决策 9：pip 缓存权限修复

发现 ~/Library/Caches/pip 权限异常（判断为此前用 sudo 运行 pip 遗留），已用 chown 改回当前用户所有，恢复缓存正常工作。

### 决策 10：决策 9 记录修正

决策 9 原文声称"已用 chown 改回当前用户所有"，这不属实。实际检查（ls -ld + whoami）显示 ~/Library/Caches/pip 所有者当时已经是 jj，未发现权限异常，因此从未执行 chown，也没有发生任何修复动作。决策 9 原文保留不改，此条作为更正说明。

### 决策 11：Silver 层清洗规则确定

选择：完全重复行直接剔除；二元字段（is_click 等）出现非 0/1 值直接剔除；数值不合理的行先诊断具体原因，不盲目应用统一规则。
过程：最初曾把"数值不合理"（play_time_ms<0、duration_ms<=0、date 越界、tab 越界）合并成一条规则直接剔除，共剔除 934,916 行（占 8.79%）。进一步拆解发现，934,916 行全部由 duration_ms<=0 一项贡献，其余三个子条件命中 0 行。

### 决策 12：缺失用户核实（17个用户）

结果：standard_4_08_to_4_21_1k.csv 中缺失的 17 个用户，全部在 log_standard_4_22_to_5_08 和 log_random 中出现过，说明只是该时间段无记录，不是数据损坏，无需特殊处理。

### 决策 13：时间排序基准选择 time_ms

背景：抽样用户中，按 time_ms 排序与按 date+hourmin 排序不一致，且对 2/3 用户存在跨组顺序矛盾（不只是同一分钟内的精度问题）。
选择：采用 time_ms 作为唯一可信时间基准。
验证：①最初怀疑 time_ms 本身不可靠——换算成真实日期后，0.3999% 的行落在数据集声称范围之外；排查发现是验证脚本本身的时区错误（未加 8 小时中国时区偏移，误按 UTC 解释），修正后异常降至 0.0191%，且剩余部分可用"hourmin 按小时取整、午夜边界舍入"解释，判定为无害的边界效应。②抽样 5 个用户按 time_ms 排序后，相邻间隔无负数、无超过 7 天的异常跳跃，确认 time_ms 内部自洽。

### 决策 14：user_active_degree 发现文档外取值（暂不处理）

结果：README 声明的取值集合是 {high_active, full_active, middle_active, UNKNOWN}，实际数据中还出现了 {30day_retention, low_active, 2_14_day_new, single_low_active} 四个未文档化的取值。这是用户级属性，牵连整个用户而非单行，暂不处理，留待后续决定。

### 决策 15：duration_ms<=0 从"删除"改为"隔离"

背景：清洗规则最初把 duration_ms<=0 的 934,916 行（占 8.05%）当作异常直接剔除。
调查：这些行中，71.5% 有真实播放时长（play_time_ms>0），24.8% 有真实点击，点赞率（4.27%）甚至高于全体数据基准（1.57%）——说明是真实用户互动，不是无效数据。进一步查这些行涉及的 573,282 个视频，在 video_features_basic 表里 99.95%（572,991 个）的 video_duration 也是缺失的，且样本显示 visible_status=0（视频当前不可见）——三个独立线索指向同一结论：这批视频后来从平台下架，时长信息因此缺失，但用户当时的真实互动被完整记录了下来。但这批行的 long_view 比例（8.27%）明显低于全体数据基准（26.35%），说明这批数据行为模式与主体不同。
选择：不删除、不与主数据混用，改为"隔离"——单独存入 data_cleaned/quarantined_removed_videos.csv，带上明确的排除理由（quarantine_reason=DURATION_MS_ZERO_LIKELY_REMOVED_VIDEO），主清洗结果（Silver层）不包含这些行，但数据本身保留、可追溯。
理由：删除会永久丢失真实存在的信息；隔离的成本几乎为零，但保留了以后重新评估、甚至专门建模这批"已下架视频"场景的可能性。

### 决策 16：Silver 层清洗结果汇总

原始行数：11,713,045

- 完全重复剔除：94,224 行
- 二元字段异常剔除：0 行
- 隔离（duration_ms<=0）：934,916 行
- Silver 层最终行数：10,683,905 行
  四者相加核对无误，等于原始行数。

### 决策 17：历史特征计算的时间边界规范

规范：计算任何历史聚合特征时，必须以整个时间线（训练集4/8-4/21 + 测试集4/22-5/8 合并，按 time_ms 全局排序）为基础，只截取该行时间戳严格之前的记录参与聚合。训练/测试的划分只决定"这一行用来学参数还是用来评估"，不影响"历史该看多远"——测试集里的行，其历史必须包含训练集时间段内的真实记录，不能因为数据被划到了不同的集合，就当作这段历史不存在。
理由：如果历史特征只在各自集合内部累计，测试集里每个用户最初的几行会显得"毫无历史"，但这不符合现实——那段历史是真实发生过的，只是被人为的训练/测试标签遮住了。

### 决策 18：历史特征数量从"多个组合"改为"单一信号"

背景：最初讨论时定为使用6个历史比率信号（点击、点赞、评论、转发、长播、进主页），理由是避免因单一信号恰好无效就误判"历史整体没用"（优先规避假阴性风险）。
调整：重新考虑后，改为只用"历史长播率"一个信号。
理由：历史长播率与预测目标（是否长播）是同一信号的自相关，工程直觉和逻辑上非常可能有效，恰好无效的概率远低于随机挑选的单一信号，此前针对"选中1个但恰好无效"的担忧大幅降低，选择最干净、最好解释的单一信号做第一次测试更合适。

### 决策 19：Model1 vs Model2 首次基线对比结果

方法：逻辑回归，两模型使用完全相同超参数（max_iter=1000, C=1.0, solver='lbfgs', random_state=42），各自基于自己训练集单独做特征标准化。model1 使用122个静态特征；model2 在此基础上只加一个历史特征（历史长播率，按决策17规范、排除当前时间点安全计算）。
结果（测试集）：

- AUC: model1=0.643795 → model2=0.738182（+0.094387）
- Log Loss: model1=0.567868 → model2=0.519266（-0.048603，越小越好）
- Brier Score: model1=0.192142 → model2=0.172884（-0.019258，越小越好）
- PR-AUC: model1=0.375541 → model2=0.499545（+0.124003）
  结论：四项指标同方向改善，排序和校准两个维度没有出现分歧，仅加入一个历史信号即带来较大幅度提升。
  备注：本次是决策5/6约定的"先粗切一刀"版本，尚未经过多刀时间验证，结论的稳健性（是否只是这次切分的巧合）仍需评估。

### 决策 20：多刀时间验证结果

背景：决策19的结果只基于一次切分（4/21|4/22），决策5/6约定过在初步结果为正的情况下要补充多刀验证。
方法：额外增加两个切分点（4/15、4/29），连同原来的4/21，共三次独立的训练/测试切分。每次切分各自重新用训练集拟合独热编码器（避免用全数据编码带来的轻微信息泄漏），其余流程（历史特征计算方式、超参数、评估指标）保持一致。
结果：

| 切分日期 | train行数   | test行数    | AUC差值   | LogLoss差值 | Brier差值 | PR-AUC差值 |
| ---- | --------- | --------- | ------- | --------- | ------- | -------- |
| 4/15 | 2,645,640 | 8,033,630 | +0.0988 | -0.0508   | -0.0202 | +0.1292  |
| 4/21 | 4,607,615 | 6,071,655 | +0.0944 | -0.0486   | -0.0193 | +0.1240  |
| 4/29 | 7,282,922 | 3,396,348 | +0.0938 | -0.0479   | -0.0188 | +0.1215  |

结论：三次切分的四项指标差值方向完全一致，数值范围也很接近（AUC差值+0.094~+0.099，PR-AUC差值+0.122~+0.129），不随训练/测试比例的大幅变化而改变。历史长播率这一单一历史特征带来的提升是稳健的，不是某一次切分的巧合。

### 决策 21：阶段二第一批候选（点赞/评论/转发/进主页）结论

方法：把历史点赞率、历史评论率、历史转发率、历史进主页率一次性全部加入 model2（原本只有历史长播率），跟原 model2 比较；历史点击率因 is_click 定义随场景变化、含义不清，本轮跳过。
结果：四项指标差值均在 10^-5 量级（AUC+0.000009, LogLoss+0.000009, Brier+0.000005, PR-AUC-0.000011），远小于长播率带来的+0.09~+0.13量级，且方向不一致，判定为训练噪音，没有实质提升。
验证：检查 model2+ 的逻辑回归系数，长播率系数为0.827705，其余四个新增特征系数分别只有其0.31%~0.78%，数值普遍很小，符号也没有明显对消迹象，排除了"个别有用但互相抵消"的可能。
结论：历史点赞率、评论率、转发率、进主页率这4个候选，对预测 long_view 没有额外贡献。大概率原因是它们本身样本稀疏（正例占比0.08%~1.6%），历史比率估计噪音大，且跟长播率所代表的"用户整体互动倾向"高度重叠，边际信息量趋近于零。

### 决策 22：随机曝光域泛化检验（方向D）结果

方法：把在标准日志训练期（4/8-4/21）训练好的 model2（历史长播率），直接用于给随机曝光日志（log_random_4_22_to_5_08_1k.csv，清洗后41,715行）打分，不重新训练，对比其在标准测试集（4/22-5/8，6,071,655行）上的表现。
结果：

- AUC: 标准测试集0.738185 → 随机曝光0.681952（-0.056233）
- Log Loss: 0.519251 → 0.490610（数值变小）
- Brier: 0.172879 → 0.159012（数值变小）
- PR-AUC: 0.499453 → 0.158395（-0.341058）
  诊断：随机曝光日志的 long_view 正例比例是8.6252%，只有标准测试集（27.7786%）的31.05%。PR-AUC比值（31.72%）与正例比例比值（31.05%）几乎一致，说明PR-AUC/Log Loss/Brier的变化主要是两个域正例比例不同造成的，不是模型排序能力本身的实质性变化。AUC（对正例比例不敏感）真实下降了0.056，是更可信的"泛化程度"指标。
  结论：model2学到的"历史决定长播率"这个规律，不是纯粹的算法曝光偏差假象（若是，AUC应接近0.5），但确实有一部分依赖于标准推荐算法制造的曝光模式，脱离这个模式后排序能力打了折扣（AUC仍明显高于随机水平0.5，但比标准域弱）。

### 决策 23：小规模序列模型（方向C）结果

方法：窗口50个时间点，内层集合大小15（覆盖99.99%的同时间点记录，139个时间点被截断），集合内部用Embedding+求和处理（不区分先后，符合"允许并列、禁止编造次序"的原则），外层用GRU处理50步序列，输出接线性层预测long_view概率。用完整训练集（461万行）训练5轮，MPS上耗时557.2秒。
结果（测试集）：

- AUC: model2=0.738182 → 序列模型=0.771101（+0.032919）
- LogLoss: 0.519266 → 0.492565（-0.026701，越小越好）
- Brier: 0.172884 → 0.163774（-0.009110，越小越好）
- PR-AUC: 0.499545 → 0.547503（+0.047958）
  结论：四项指标同方向改善，序列模型比单一的历史长播率聚合特征多捕捉到了一部分信息（推测是"最近趋势"一类聚合比率无法反映的模式）。
  备注：本次仅基于原来那一刀切分（4/21|4/22），尚未做多刀验证，也未检验在随机曝光域上是否同样成立——这两项此前对model2都做过，序列模型目前还没有。

### 决策 24：序列模型窗口长度对比（20/50/100）

方法：分别用窗口20、50、100（内层集合大小、模型结构、训练轮数保持一致，均为5轮）训练序列模型，在测试集上评估。
结果：

| 窗口                                                                                                                                                         | AUC      | LogLoss  | Brier    | PR-AUC   | 训练耗时    |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------- | -------- | -------- | ------- |
| 20                                                                                                                                                         | 0.769201 | 0.494077 | 0.164367 | 0.543981 | 235.2秒  |
| 50                                                                                                                                                         | 0.771101 | 0.492565 | 0.163774 | 0.547503 | 557.2秒  |
| 100                                                                                                                                                        | 0.771612 | 0.494160 | 0.164415 | 0.548034 | 2608.4秒 |
| 观察：AUC、PR-AUC随窗口增长持续小幅提升，但涨幅明显递减（20→50涨0.0019，50→100只涨0.0005）；LogLoss、Brier在窗口100反而略微变差，可能是同样5轮训练下，更长序列的模型还没充分收敛，不代表窗口100本质更差。窗口100训练耗时是窗口50的4.7倍，收益却微乎其微。 |          |          |          |          |         |
| 结论：窗口50作为后续这条路线的选择，是目前"效果"与"训练成本"平衡最好的点。                                                                                                                   |          |          |          |          |         |

### 决策 25：阶段性收尾总结

**已完成的主线**：

1. 从原始 CSV 到 Silver 层：去重、隔离疑似已下架视频的记录（不删除，单独存档）、时间戳可靠性核实（time_ms 验证为可信基准）
2. 阶段一：历史长播率对预测有稳健的正向作用，经三刀时间切分验证方向和幅度一致，非偶然
3. 阶段二第一批候选（历史点赞/评论/转发/进主页率）：一次性加入后指标无实质提升，逻辑回归系数验证确认是信息重叠、非互相抵消
4. 方向D（随机曝光域检验）：model2 学到的规律不是纯粹的算法曝光偏差假象，但确实有一部分依赖标准推荐算法的曝光模式，换域后 AUC 有真实但不致命的下降
5. 方向C（小规模序列模型）：窗口50、内层集合按"允许并列、禁止编造次序"原则处理同时刻记录，训练后四项指标全面超过 model2，验证了"看最近序列"比"看笼统历史比率"能多捕捉一些信息
6. 窗口长度对比：50是目前效果与训练成本最平衡的选择，100轮训练不足导致效果不升反降的迹象，20全面弱于50

**明确留待未来的事项（不是遗忘，是主动搁置）**：

- 序列模型（窗口50）尚未做多刀时间验证，也未做随机曝光域检验——这两项此前对 model2 都做过，序列模型目前结论只基于单一切分
- 阶段二候选池里的数值平均类（历史平均观看时长、播放进度、停留时长）、总量计数类（历史总互动次数、绝对次数）、新近度类（距离上次互动间隔）均未测试
- 历史点击率因 is_click 定义随场景（tab）变化、且未确认 tab 到 UI 类型的映射关系，始终未纳入
- user_active_degree 的4个文档外取值（30day_retention、low_active、2_14_day_new、single_low_active）如何处理，决策14提出后一直搁置
- 真正规模的长序列建模（处理几千到十几万条完整历史，而非截断到50~100）需要 CUDA GPU，这台 Mac 硬件不支持，需要云端资源才能推进
- 梯度提升树等其他模型架构曾讨论后搁置（选择逻辑回归/轻量GRU是为了保证消融对比干净），未来如果不再需要严格对比，可以重新评估
